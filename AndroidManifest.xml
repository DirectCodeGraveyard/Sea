<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.neo.sea" >

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <permission
        android:name="org.neo.sea.PROVIDE_MODULE"
        android:description="@string/perm_provide_module_desc"
        android:label="@string/perm_provide_module_label" />

    <application
        android:name=".core.SeaApplication"
        android:allowBackup="true"
        android:icon="@android:drawable/ic_menu_more"
        android:label="@string/app_name" >
        <activity
            android:name=".ui.ModuleToggleActivity"
            android:enabled="true"
            android:exported="true"
            android:label="Sea Module Toggler" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.FirstRunActivity"
            android:label="@string/title_activity_first_run"
            android:theme="@android:style/Theme.Material.NoActionBar">
        </activity>

        <activity android:name=".ui.RuleManagerActivity" android:label="Sea Rule Manager" android:theme="@android:style/Theme.Material" />

        <service
            android:name=".core.SeaService"
            android:enabled="true" />

        <receiver
            android:name=".core.BootReceiver"
            android:enabled="true"
            android:label="StartSeaOnBoot" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".core.PackageReceiver"
            android:enabled="true"
            android:label="PackageReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
