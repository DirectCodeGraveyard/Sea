<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.directcode.neo.sea">

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="21"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".SeaApplication"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_more"
        android:allowBackup="true">

        <activity android:exported="true" android:name=".ControllerActivity" android:enabled="true" android:label="Sea Controller">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service android:name=".SeaService" android:enabled="true" />

        <receiver android:name=".BootReceiver" android:enabled="true" android:label="StartSeaOnBoot">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>

